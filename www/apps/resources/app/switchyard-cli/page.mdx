---
generate_toc: true
---

import { Table, Prerequisites, ChildDocs } from "docs-ui"

export const metadata = {
  title: `Switchyard CLI Reference`,
}

# {metadata.title}

The Switchyard CLI tool provides commands that facilitate your development.

<Prerequisites items={[
  {
    text: "Node.js v20+",
    link: "https://nodejs.org/en/download"
  },
  {
    text: "Git CLI tool",
    link: "https://git-scm.com/downloads"
  },
  {
    text: "PostgreSQL",
    link: "https://www.postgresql.org/download/"
  }
]} />

## Usage

In your Switchyard application's directory, you can use the Switchyard CLI tool using NPX.

For example:

```bash
npx switchyard --help
```

---

<ChildDocs hideItems={["Overview"]} />