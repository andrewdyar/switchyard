import { ChildDocs } from "docs-ui"

export const metadata = {
  title: `Currency JS SDK Methods`,
}

# {metadata.title}

The [JS SDK](../../../js-sdk/page.mdx) allows you to send requests to the Switchyard server application from your client applications, such as a storefront or the Switchyard Admin dashboard.

Use the following methods or properties to send requests to currency related API routes in your Switchyard application. They are provided by the `@switchyard/js-sdk` package.

The following links will take you to the reference documentation for the [JS SDK](../../../js-sdk/page.mdx).

<ChildDocs type="item" />