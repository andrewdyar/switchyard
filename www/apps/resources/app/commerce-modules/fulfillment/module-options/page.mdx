---
sidebar_label: "Module Options"
---

import { Table } from "docs-ui"

export const metadata = {
  title: `Fulfillment Module Options`,
}

# {metadata.title}

In this document, you'll learn about the options of the Fulfillment Module. You can pass these options in `switchyard.config.ts`.

## providers

The `providers` option is an array of [Fulfillment Module Providers](../fulfillment-provider/page.mdx).

When the Switchyard application starts, these providers are registered and can be used to process fulfillments.

For example:

```ts title="switchyard.config.ts"
import { Modules } from "@switchyard/framework/utils"

// ...

module.exports = defineConfig({
  // ...
  modules: [
    {
      resolve: "@switchyard/medusa/fulfillment",
      options: {
        providers: [
          {
            resolve: `@switchyard/medusa/fulfillment-manual`,
            id: "manual",
            options: {
              // provider options...
            },
          },
        ],
      },
    },
  ],
})
```

The `providers` option is an array of objects that accept the following properties:

- `resolve`: A string indicating either the package name  of the module provider or its relative path.
- `id`: A string indicating the provider's unique name or ID.
- `options`: An optional object of the module provider's options.
