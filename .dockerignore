# =============================================================================
# Docker Ignore for CI/CD Pre-built Deployment
# =============================================================================
# Since we only copy apps/goods-backend/.medusa/server, most files are ignored.
# This file ensures minimal build context for faster Docker builds.
# =============================================================================

# Ignore everything by default
*

# Only include the pre-built Medusa server
!apps/goods-backend/.medusa/server
!apps/goods-backend/.medusa/server/**
